import { Construccion } from '@/components/curso/etapas/Construccion';

export const metadata = { title: 'Construye tu prompt · Etapa 2' };

export default function PromptPage() {
  return <Construccion />;
}
